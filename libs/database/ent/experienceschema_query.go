// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"persona/libs/database/ent/experienceschema"
	"persona/libs/database/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExperienceSchemaQuery is the builder for querying ExperienceSchema entities.
type ExperienceSchemaQuery struct {
	config
	ctx        *QueryContext
	order      []experienceschema.OrderOption
	inters     []Interceptor
	predicates []predicate.ExperienceSchema
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExperienceSchemaQuery builder.
func (esq *ExperienceSchemaQuery) Where(ps ...predicate.ExperienceSchema) *ExperienceSchemaQuery {
	esq.predicates = append(esq.predicates, ps...)
	return esq
}

// Limit the number of records to be returned by this query.
func (esq *ExperienceSchemaQuery) Limit(limit int) *ExperienceSchemaQuery {
	esq.ctx.Limit = &limit
	return esq
}

// Offset to start from.
func (esq *ExperienceSchemaQuery) Offset(offset int) *ExperienceSchemaQuery {
	esq.ctx.Offset = &offset
	return esq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (esq *ExperienceSchemaQuery) Unique(unique bool) *ExperienceSchemaQuery {
	esq.ctx.Unique = &unique
	return esq
}

// Order specifies how the records should be ordered.
func (esq *ExperienceSchemaQuery) Order(o ...experienceschema.OrderOption) *ExperienceSchemaQuery {
	esq.order = append(esq.order, o...)
	return esq
}

// First returns the first ExperienceSchema entity from the query.
// Returns a *NotFoundError when no ExperienceSchema was found.
func (esq *ExperienceSchemaQuery) First(ctx context.Context) (*ExperienceSchema, error) {
	nodes, err := esq.Limit(1).All(setContextOp(ctx, esq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{experienceschema.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) FirstX(ctx context.Context) *ExperienceSchema {
	node, err := esq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExperienceSchema ID from the query.
// Returns a *NotFoundError when no ExperienceSchema ID was found.
func (esq *ExperienceSchemaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = esq.Limit(1).IDs(setContextOp(ctx, esq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{experienceschema.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) FirstIDX(ctx context.Context) int {
	id, err := esq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExperienceSchema entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExperienceSchema entity is found.
// Returns a *NotFoundError when no ExperienceSchema entities are found.
func (esq *ExperienceSchemaQuery) Only(ctx context.Context) (*ExperienceSchema, error) {
	nodes, err := esq.Limit(2).All(setContextOp(ctx, esq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{experienceschema.Label}
	default:
		return nil, &NotSingularError{experienceschema.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) OnlyX(ctx context.Context) *ExperienceSchema {
	node, err := esq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExperienceSchema ID in the query.
// Returns a *NotSingularError when more than one ExperienceSchema ID is found.
// Returns a *NotFoundError when no entities are found.
func (esq *ExperienceSchemaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = esq.Limit(2).IDs(setContextOp(ctx, esq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{experienceschema.Label}
	default:
		err = &NotSingularError{experienceschema.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) OnlyIDX(ctx context.Context) int {
	id, err := esq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExperienceSchemas.
func (esq *ExperienceSchemaQuery) All(ctx context.Context) ([]*ExperienceSchema, error) {
	ctx = setContextOp(ctx, esq.ctx, "All")
	if err := esq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExperienceSchema, *ExperienceSchemaQuery]()
	return withInterceptors[[]*ExperienceSchema](ctx, esq, qr, esq.inters)
}

// AllX is like All, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) AllX(ctx context.Context) []*ExperienceSchema {
	nodes, err := esq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExperienceSchema IDs.
func (esq *ExperienceSchemaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if esq.ctx.Unique == nil && esq.path != nil {
		esq.Unique(true)
	}
	ctx = setContextOp(ctx, esq.ctx, "IDs")
	if err = esq.Select(experienceschema.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) IDsX(ctx context.Context) []int {
	ids, err := esq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (esq *ExperienceSchemaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, esq.ctx, "Count")
	if err := esq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, esq, querierCount[*ExperienceSchemaQuery](), esq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) CountX(ctx context.Context) int {
	count, err := esq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (esq *ExperienceSchemaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, esq.ctx, "Exist")
	switch _, err := esq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (esq *ExperienceSchemaQuery) ExistX(ctx context.Context) bool {
	exist, err := esq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExperienceSchemaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (esq *ExperienceSchemaQuery) Clone() *ExperienceSchemaQuery {
	if esq == nil {
		return nil
	}
	return &ExperienceSchemaQuery{
		config:     esq.config,
		ctx:        esq.ctx.Clone(),
		order:      append([]experienceschema.OrderOption{}, esq.order...),
		inters:     append([]Interceptor{}, esq.inters...),
		predicates: append([]predicate.ExperienceSchema{}, esq.predicates...),
		// clone intermediate query.
		sql:  esq.sql.Clone(),
		path: esq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (esq *ExperienceSchemaQuery) GroupBy(field string, fields ...string) *ExperienceSchemaGroupBy {
	esq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExperienceSchemaGroupBy{build: esq}
	grbuild.flds = &esq.ctx.Fields
	grbuild.label = experienceschema.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (esq *ExperienceSchemaQuery) Select(fields ...string) *ExperienceSchemaSelect {
	esq.ctx.Fields = append(esq.ctx.Fields, fields...)
	sbuild := &ExperienceSchemaSelect{ExperienceSchemaQuery: esq}
	sbuild.label = experienceschema.Label
	sbuild.flds, sbuild.scan = &esq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExperienceSchemaSelect configured with the given aggregations.
func (esq *ExperienceSchemaQuery) Aggregate(fns ...AggregateFunc) *ExperienceSchemaSelect {
	return esq.Select().Aggregate(fns...)
}

func (esq *ExperienceSchemaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range esq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, esq); err != nil {
				return err
			}
		}
	}
	for _, f := range esq.ctx.Fields {
		if !experienceschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if esq.path != nil {
		prev, err := esq.path(ctx)
		if err != nil {
			return err
		}
		esq.sql = prev
	}
	return nil
}

func (esq *ExperienceSchemaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExperienceSchema, error) {
	var (
		nodes = []*ExperienceSchema{}
		_spec = esq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExperienceSchema).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExperienceSchema{config: esq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, esq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (esq *ExperienceSchemaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := esq.querySpec()
	_spec.Node.Columns = esq.ctx.Fields
	if len(esq.ctx.Fields) > 0 {
		_spec.Unique = esq.ctx.Unique != nil && *esq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, esq.driver, _spec)
}

func (esq *ExperienceSchemaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(experienceschema.Table, experienceschema.Columns, sqlgraph.NewFieldSpec(experienceschema.FieldID, field.TypeInt))
	_spec.From = esq.sql
	if unique := esq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if esq.path != nil {
		_spec.Unique = true
	}
	if fields := esq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, experienceschema.FieldID)
		for i := range fields {
			if fields[i] != experienceschema.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := esq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := esq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := esq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := esq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (esq *ExperienceSchemaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(esq.driver.Dialect())
	t1 := builder.Table(experienceschema.Table)
	columns := esq.ctx.Fields
	if len(columns) == 0 {
		columns = experienceschema.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if esq.sql != nil {
		selector = esq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if esq.ctx.Unique != nil && *esq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range esq.predicates {
		p(selector)
	}
	for _, p := range esq.order {
		p(selector)
	}
	if offset := esq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := esq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExperienceSchemaGroupBy is the group-by builder for ExperienceSchema entities.
type ExperienceSchemaGroupBy struct {
	selector
	build *ExperienceSchemaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (esgb *ExperienceSchemaGroupBy) Aggregate(fns ...AggregateFunc) *ExperienceSchemaGroupBy {
	esgb.fns = append(esgb.fns, fns...)
	return esgb
}

// Scan applies the selector query and scans the result into the given value.
func (esgb *ExperienceSchemaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, esgb.build.ctx, "GroupBy")
	if err := esgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExperienceSchemaQuery, *ExperienceSchemaGroupBy](ctx, esgb.build, esgb, esgb.build.inters, v)
}

func (esgb *ExperienceSchemaGroupBy) sqlScan(ctx context.Context, root *ExperienceSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(esgb.fns))
	for _, fn := range esgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*esgb.flds)+len(esgb.fns))
		for _, f := range *esgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*esgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := esgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExperienceSchemaSelect is the builder for selecting fields of ExperienceSchema entities.
type ExperienceSchemaSelect struct {
	*ExperienceSchemaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ess *ExperienceSchemaSelect) Aggregate(fns ...AggregateFunc) *ExperienceSchemaSelect {
	ess.fns = append(ess.fns, fns...)
	return ess
}

// Scan applies the selector query and scans the result into the given value.
func (ess *ExperienceSchemaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ess.ctx, "Select")
	if err := ess.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExperienceSchemaQuery, *ExperienceSchemaSelect](ctx, ess.ExperienceSchemaQuery, ess, ess.inters, v)
}

func (ess *ExperienceSchemaSelect) sqlScan(ctx context.Context, root *ExperienceSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ess.fns))
	for _, fn := range ess.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ess.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ess.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
