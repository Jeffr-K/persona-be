// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"persona/libs/database/ent/phoneschema"
	"persona/libs/database/ent/profileschema"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PhoneSchemaCreate is the builder for creating a PhoneSchema entity.
type PhoneSchemaCreate struct {
	config
	mutation *PhoneSchemaMutation
	hooks    []Hook
}

// SetOwnerID sets the "owner" edge to the ProfileSchema entity by ID.
func (psc *PhoneSchemaCreate) SetOwnerID(id int) *PhoneSchemaCreate {
	psc.mutation.SetOwnerID(id)
	return psc
}

// SetOwner sets the "owner" edge to the ProfileSchema entity.
func (psc *PhoneSchemaCreate) SetOwner(p *ProfileSchema) *PhoneSchemaCreate {
	return psc.SetOwnerID(p.ID)
}

// Mutation returns the PhoneSchemaMutation object of the builder.
func (psc *PhoneSchemaCreate) Mutation() *PhoneSchemaMutation {
	return psc.mutation
}

// Save creates the PhoneSchema in the database.
func (psc *PhoneSchemaCreate) Save(ctx context.Context) (*PhoneSchema, error) {
	return withHooks(ctx, psc.sqlSave, psc.mutation, psc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (psc *PhoneSchemaCreate) SaveX(ctx context.Context) *PhoneSchema {
	v, err := psc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (psc *PhoneSchemaCreate) Exec(ctx context.Context) error {
	_, err := psc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (psc *PhoneSchemaCreate) ExecX(ctx context.Context) {
	if err := psc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (psc *PhoneSchemaCreate) check() error {
	if _, ok := psc.mutation.OwnerID(); !ok {
		return &ValidationError{Name: "owner", err: errors.New(`ent: missing required edge "PhoneSchema.owner"`)}
	}
	return nil
}

func (psc *PhoneSchemaCreate) sqlSave(ctx context.Context) (*PhoneSchema, error) {
	if err := psc.check(); err != nil {
		return nil, err
	}
	_node, _spec := psc.createSpec()
	if err := sqlgraph.CreateNode(ctx, psc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	psc.mutation.id = &_node.ID
	psc.mutation.done = true
	return _node, nil
}

func (psc *PhoneSchemaCreate) createSpec() (*PhoneSchema, *sqlgraph.CreateSpec) {
	var (
		_node = &PhoneSchema{config: psc.config}
		_spec = sqlgraph.NewCreateSpec(phoneschema.Table, sqlgraph.NewFieldSpec(phoneschema.FieldID, field.TypeInt))
	)
	if nodes := psc.mutation.OwnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   phoneschema.OwnerTable,
			Columns: []string{phoneschema.OwnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(profileschema.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.profile_schema_phone = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PhoneSchemaCreateBulk is the builder for creating many PhoneSchema entities in bulk.
type PhoneSchemaCreateBulk struct {
	config
	builders []*PhoneSchemaCreate
}

// Save creates the PhoneSchema entities in the database.
func (pscb *PhoneSchemaCreateBulk) Save(ctx context.Context) ([]*PhoneSchema, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pscb.builders))
	nodes := make([]*PhoneSchema, len(pscb.builders))
	mutators := make([]Mutator, len(pscb.builders))
	for i := range pscb.builders {
		func(i int, root context.Context) {
			builder := pscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PhoneSchemaMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pscb *PhoneSchemaCreateBulk) SaveX(ctx context.Context) []*PhoneSchema {
	v, err := pscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pscb *PhoneSchemaCreateBulk) Exec(ctx context.Context) error {
	_, err := pscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pscb *PhoneSchemaCreateBulk) ExecX(ctx context.Context) {
	if err := pscb.Exec(ctx); err != nil {
		panic(err)
	}
}
