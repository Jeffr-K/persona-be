// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"persona/libs/database/ent/logoschema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// LogoSchema is the model entity for the LogoSchema schema.
type LogoSchema struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LogoSchema) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case logoschema.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LogoSchema fields.
func (ls *LogoSchema) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case logoschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ls.ID = int(value.Int64)
		default:
			ls.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LogoSchema.
// This includes values selected through modifiers, order, etc.
func (ls *LogoSchema) Value(name string) (ent.Value, error) {
	return ls.selectValues.Get(name)
}

// Update returns a builder for updating this LogoSchema.
// Note that you need to call LogoSchema.Unwrap() before calling this method if this LogoSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (ls *LogoSchema) Update() *LogoSchemaUpdateOne {
	return NewLogoSchemaClient(ls.config).UpdateOne(ls)
}

// Unwrap unwraps the LogoSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ls *LogoSchema) Unwrap() *LogoSchema {
	_tx, ok := ls.config.driver.(*txDriver)
	if !ok {
		panic("ent: LogoSchema is not a transactional entity")
	}
	ls.config.driver = _tx.drv
	return ls
}

// String implements the fmt.Stringer.
func (ls *LogoSchema) String() string {
	var builder strings.Builder
	builder.WriteString("LogoSchema(")
	builder.WriteString(fmt.Sprintf("id=%v", ls.ID))
	builder.WriteByte(')')
	return builder.String()
}

// LogoSchemas is a parsable slice of LogoSchema.
type LogoSchemas []*LogoSchema
