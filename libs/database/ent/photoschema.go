// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"persona/libs/database/ent/photoschema"
	"persona/libs/database/ent/profileschema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PhotoSchema is the model entity for the PhotoSchema schema.
type PhotoSchema struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PhotoSchemaQuery when eager-loading is set.
	Edges                PhotoSchemaEdges `json:"edges"`
	profile_schema_photo *int
	selectValues         sql.SelectValues
}

// PhotoSchemaEdges holds the relations/edges for other nodes in the graph.
type PhotoSchemaEdges struct {
	// Owner holds the value of the owner edge.
	Owner *ProfileSchema `json:"owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PhotoSchemaEdges) OwnerOrErr() (*ProfileSchema, error) {
	if e.loadedTypes[0] {
		if e.Owner == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: profileschema.Label}
		}
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PhotoSchema) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case photoschema.FieldID:
			values[i] = new(sql.NullInt64)
		case photoschema.ForeignKeys[0]: // profile_schema_photo
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PhotoSchema fields.
func (ps *PhotoSchema) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case photoschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ps.ID = int(value.Int64)
		case photoschema.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field profile_schema_photo", value)
			} else if value.Valid {
				ps.profile_schema_photo = new(int)
				*ps.profile_schema_photo = int(value.Int64)
			}
		default:
			ps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PhotoSchema.
// This includes values selected through modifiers, order, etc.
func (ps *PhotoSchema) Value(name string) (ent.Value, error) {
	return ps.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the PhotoSchema entity.
func (ps *PhotoSchema) QueryOwner() *ProfileSchemaQuery {
	return NewPhotoSchemaClient(ps.config).QueryOwner(ps)
}

// Update returns a builder for updating this PhotoSchema.
// Note that you need to call PhotoSchema.Unwrap() before calling this method if this PhotoSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (ps *PhotoSchema) Update() *PhotoSchemaUpdateOne {
	return NewPhotoSchemaClient(ps.config).UpdateOne(ps)
}

// Unwrap unwraps the PhotoSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ps *PhotoSchema) Unwrap() *PhotoSchema {
	_tx, ok := ps.config.driver.(*txDriver)
	if !ok {
		panic("ent: PhotoSchema is not a transactional entity")
	}
	ps.config.driver = _tx.drv
	return ps
}

// String implements the fmt.Stringer.
func (ps *PhotoSchema) String() string {
	var builder strings.Builder
	builder.WriteString("PhotoSchema(")
	builder.WriteString(fmt.Sprintf("id=%v", ps.ID))
	builder.WriteByte(')')
	return builder.String()
}

// PhotoSchemas is a parsable slice of PhotoSchema.
type PhotoSchemas []*PhotoSchema
