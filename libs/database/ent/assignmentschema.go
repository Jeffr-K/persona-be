// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"persona/libs/database/ent/assignmentschema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// AssignmentSchema is the model entity for the AssignmentSchema schema.
type AssignmentSchema struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Type holds the value of the "type" field.
	Type         string `json:"type,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AssignmentSchema) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case assignmentschema.FieldID:
			values[i] = new(sql.NullInt64)
		case assignmentschema.FieldName, assignmentschema.FieldType:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AssignmentSchema fields.
func (as *AssignmentSchema) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case assignmentschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			as.ID = int(value.Int64)
		case assignmentschema.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				as.Name = value.String
			}
		case assignmentschema.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				as.Type = value.String
			}
		default:
			as.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AssignmentSchema.
// This includes values selected through modifiers, order, etc.
func (as *AssignmentSchema) Value(name string) (ent.Value, error) {
	return as.selectValues.Get(name)
}

// Update returns a builder for updating this AssignmentSchema.
// Note that you need to call AssignmentSchema.Unwrap() before calling this method if this AssignmentSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (as *AssignmentSchema) Update() *AssignmentSchemaUpdateOne {
	return NewAssignmentSchemaClient(as.config).UpdateOne(as)
}

// Unwrap unwraps the AssignmentSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (as *AssignmentSchema) Unwrap() *AssignmentSchema {
	_tx, ok := as.config.driver.(*txDriver)
	if !ok {
		panic("ent: AssignmentSchema is not a transactional entity")
	}
	as.config.driver = _tx.drv
	return as
}

// String implements the fmt.Stringer.
func (as *AssignmentSchema) String() string {
	var builder strings.Builder
	builder.WriteString("AssignmentSchema(")
	builder.WriteString(fmt.Sprintf("id=%v, ", as.ID))
	builder.WriteString("name=")
	builder.WriteString(as.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(as.Type)
	builder.WriteByte(')')
	return builder.String()
}

// AssignmentSchemas is a parsable slice of AssignmentSchema.
type AssignmentSchemas []*AssignmentSchema
