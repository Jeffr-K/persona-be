// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"persona/libs/database/ent/photoschema"
	"persona/libs/database/ent/predicate"
	"persona/libs/database/ent/profileschema"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PhotoSchemaQuery is the builder for querying PhotoSchema entities.
type PhotoSchemaQuery struct {
	config
	ctx        *QueryContext
	order      []photoschema.OrderOption
	inters     []Interceptor
	predicates []predicate.PhotoSchema
	withOwner  *ProfileSchemaQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PhotoSchemaQuery builder.
func (psq *PhotoSchemaQuery) Where(ps ...predicate.PhotoSchema) *PhotoSchemaQuery {
	psq.predicates = append(psq.predicates, ps...)
	return psq
}

// Limit the number of records to be returned by this query.
func (psq *PhotoSchemaQuery) Limit(limit int) *PhotoSchemaQuery {
	psq.ctx.Limit = &limit
	return psq
}

// Offset to start from.
func (psq *PhotoSchemaQuery) Offset(offset int) *PhotoSchemaQuery {
	psq.ctx.Offset = &offset
	return psq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (psq *PhotoSchemaQuery) Unique(unique bool) *PhotoSchemaQuery {
	psq.ctx.Unique = &unique
	return psq
}

// Order specifies how the records should be ordered.
func (psq *PhotoSchemaQuery) Order(o ...photoschema.OrderOption) *PhotoSchemaQuery {
	psq.order = append(psq.order, o...)
	return psq
}

// QueryOwner chains the current query on the "owner" edge.
func (psq *PhotoSchemaQuery) QueryOwner() *ProfileSchemaQuery {
	query := (&ProfileSchemaClient{config: psq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := psq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := psq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(photoschema.Table, photoschema.FieldID, selector),
			sqlgraph.To(profileschema.Table, profileschema.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, photoschema.OwnerTable, photoschema.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(psq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PhotoSchema entity from the query.
// Returns a *NotFoundError when no PhotoSchema was found.
func (psq *PhotoSchemaQuery) First(ctx context.Context) (*PhotoSchema, error) {
	nodes, err := psq.Limit(1).All(setContextOp(ctx, psq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{photoschema.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (psq *PhotoSchemaQuery) FirstX(ctx context.Context) *PhotoSchema {
	node, err := psq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PhotoSchema ID from the query.
// Returns a *NotFoundError when no PhotoSchema ID was found.
func (psq *PhotoSchemaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = psq.Limit(1).IDs(setContextOp(ctx, psq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{photoschema.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (psq *PhotoSchemaQuery) FirstIDX(ctx context.Context) int {
	id, err := psq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PhotoSchema entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PhotoSchema entity is found.
// Returns a *NotFoundError when no PhotoSchema entities are found.
func (psq *PhotoSchemaQuery) Only(ctx context.Context) (*PhotoSchema, error) {
	nodes, err := psq.Limit(2).All(setContextOp(ctx, psq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{photoschema.Label}
	default:
		return nil, &NotSingularError{photoschema.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (psq *PhotoSchemaQuery) OnlyX(ctx context.Context) *PhotoSchema {
	node, err := psq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PhotoSchema ID in the query.
// Returns a *NotSingularError when more than one PhotoSchema ID is found.
// Returns a *NotFoundError when no entities are found.
func (psq *PhotoSchemaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = psq.Limit(2).IDs(setContextOp(ctx, psq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{photoschema.Label}
	default:
		err = &NotSingularError{photoschema.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (psq *PhotoSchemaQuery) OnlyIDX(ctx context.Context) int {
	id, err := psq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PhotoSchemas.
func (psq *PhotoSchemaQuery) All(ctx context.Context) ([]*PhotoSchema, error) {
	ctx = setContextOp(ctx, psq.ctx, "All")
	if err := psq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PhotoSchema, *PhotoSchemaQuery]()
	return withInterceptors[[]*PhotoSchema](ctx, psq, qr, psq.inters)
}

// AllX is like All, but panics if an error occurs.
func (psq *PhotoSchemaQuery) AllX(ctx context.Context) []*PhotoSchema {
	nodes, err := psq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PhotoSchema IDs.
func (psq *PhotoSchemaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if psq.ctx.Unique == nil && psq.path != nil {
		psq.Unique(true)
	}
	ctx = setContextOp(ctx, psq.ctx, "IDs")
	if err = psq.Select(photoschema.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (psq *PhotoSchemaQuery) IDsX(ctx context.Context) []int {
	ids, err := psq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (psq *PhotoSchemaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, psq.ctx, "Count")
	if err := psq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, psq, querierCount[*PhotoSchemaQuery](), psq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (psq *PhotoSchemaQuery) CountX(ctx context.Context) int {
	count, err := psq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (psq *PhotoSchemaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, psq.ctx, "Exist")
	switch _, err := psq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (psq *PhotoSchemaQuery) ExistX(ctx context.Context) bool {
	exist, err := psq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PhotoSchemaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (psq *PhotoSchemaQuery) Clone() *PhotoSchemaQuery {
	if psq == nil {
		return nil
	}
	return &PhotoSchemaQuery{
		config:     psq.config,
		ctx:        psq.ctx.Clone(),
		order:      append([]photoschema.OrderOption{}, psq.order...),
		inters:     append([]Interceptor{}, psq.inters...),
		predicates: append([]predicate.PhotoSchema{}, psq.predicates...),
		withOwner:  psq.withOwner.Clone(),
		// clone intermediate query.
		sql:  psq.sql.Clone(),
		path: psq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (psq *PhotoSchemaQuery) WithOwner(opts ...func(*ProfileSchemaQuery)) *PhotoSchemaQuery {
	query := (&ProfileSchemaClient{config: psq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	psq.withOwner = query
	return psq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (psq *PhotoSchemaQuery) GroupBy(field string, fields ...string) *PhotoSchemaGroupBy {
	psq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PhotoSchemaGroupBy{build: psq}
	grbuild.flds = &psq.ctx.Fields
	grbuild.label = photoschema.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (psq *PhotoSchemaQuery) Select(fields ...string) *PhotoSchemaSelect {
	psq.ctx.Fields = append(psq.ctx.Fields, fields...)
	sbuild := &PhotoSchemaSelect{PhotoSchemaQuery: psq}
	sbuild.label = photoschema.Label
	sbuild.flds, sbuild.scan = &psq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PhotoSchemaSelect configured with the given aggregations.
func (psq *PhotoSchemaQuery) Aggregate(fns ...AggregateFunc) *PhotoSchemaSelect {
	return psq.Select().Aggregate(fns...)
}

func (psq *PhotoSchemaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range psq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, psq); err != nil {
				return err
			}
		}
	}
	for _, f := range psq.ctx.Fields {
		if !photoschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if psq.path != nil {
		prev, err := psq.path(ctx)
		if err != nil {
			return err
		}
		psq.sql = prev
	}
	return nil
}

func (psq *PhotoSchemaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PhotoSchema, error) {
	var (
		nodes       = []*PhotoSchema{}
		withFKs     = psq.withFKs
		_spec       = psq.querySpec()
		loadedTypes = [1]bool{
			psq.withOwner != nil,
		}
	)
	if psq.withOwner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, photoschema.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PhotoSchema).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PhotoSchema{config: psq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, psq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := psq.withOwner; query != nil {
		if err := psq.loadOwner(ctx, query, nodes, nil,
			func(n *PhotoSchema, e *ProfileSchema) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (psq *PhotoSchemaQuery) loadOwner(ctx context.Context, query *ProfileSchemaQuery, nodes []*PhotoSchema, init func(*PhotoSchema), assign func(*PhotoSchema, *ProfileSchema)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PhotoSchema)
	for i := range nodes {
		if nodes[i].profile_schema_photo == nil {
			continue
		}
		fk := *nodes[i].profile_schema_photo
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(profileschema.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "profile_schema_photo" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (psq *PhotoSchemaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := psq.querySpec()
	_spec.Node.Columns = psq.ctx.Fields
	if len(psq.ctx.Fields) > 0 {
		_spec.Unique = psq.ctx.Unique != nil && *psq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, psq.driver, _spec)
}

func (psq *PhotoSchemaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(photoschema.Table, photoschema.Columns, sqlgraph.NewFieldSpec(photoschema.FieldID, field.TypeInt))
	_spec.From = psq.sql
	if unique := psq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if psq.path != nil {
		_spec.Unique = true
	}
	if fields := psq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, photoschema.FieldID)
		for i := range fields {
			if fields[i] != photoschema.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := psq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := psq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := psq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := psq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (psq *PhotoSchemaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(psq.driver.Dialect())
	t1 := builder.Table(photoschema.Table)
	columns := psq.ctx.Fields
	if len(columns) == 0 {
		columns = photoschema.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if psq.sql != nil {
		selector = psq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if psq.ctx.Unique != nil && *psq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range psq.predicates {
		p(selector)
	}
	for _, p := range psq.order {
		p(selector)
	}
	if offset := psq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := psq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PhotoSchemaGroupBy is the group-by builder for PhotoSchema entities.
type PhotoSchemaGroupBy struct {
	selector
	build *PhotoSchemaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (psgb *PhotoSchemaGroupBy) Aggregate(fns ...AggregateFunc) *PhotoSchemaGroupBy {
	psgb.fns = append(psgb.fns, fns...)
	return psgb
}

// Scan applies the selector query and scans the result into the given value.
func (psgb *PhotoSchemaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, psgb.build.ctx, "GroupBy")
	if err := psgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PhotoSchemaQuery, *PhotoSchemaGroupBy](ctx, psgb.build, psgb, psgb.build.inters, v)
}

func (psgb *PhotoSchemaGroupBy) sqlScan(ctx context.Context, root *PhotoSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(psgb.fns))
	for _, fn := range psgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*psgb.flds)+len(psgb.fns))
		for _, f := range *psgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*psgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := psgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PhotoSchemaSelect is the builder for selecting fields of PhotoSchema entities.
type PhotoSchemaSelect struct {
	*PhotoSchemaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pss *PhotoSchemaSelect) Aggregate(fns ...AggregateFunc) *PhotoSchemaSelect {
	pss.fns = append(pss.fns, fns...)
	return pss
}

// Scan applies the selector query and scans the result into the given value.
func (pss *PhotoSchemaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pss.ctx, "Select")
	if err := pss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PhotoSchemaQuery, *PhotoSchemaSelect](ctx, pss.PhotoSchemaQuery, pss, pss.inters, v)
}

func (pss *PhotoSchemaSelect) sqlScan(ctx context.Context, root *PhotoSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pss.fns))
	for _, fn := range pss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
