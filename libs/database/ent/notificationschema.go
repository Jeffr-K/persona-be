// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"persona/libs/database/ent/notificationschema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NotificationSchema is the model entity for the NotificationSchema schema.
type NotificationSchema struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NotificationSchema) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case notificationschema.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NotificationSchema fields.
func (ns *NotificationSchema) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case notificationschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ns.ID = int(value.Int64)
		default:
			ns.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NotificationSchema.
// This includes values selected through modifiers, order, etc.
func (ns *NotificationSchema) Value(name string) (ent.Value, error) {
	return ns.selectValues.Get(name)
}

// Update returns a builder for updating this NotificationSchema.
// Note that you need to call NotificationSchema.Unwrap() before calling this method if this NotificationSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (ns *NotificationSchema) Update() *NotificationSchemaUpdateOne {
	return NewNotificationSchemaClient(ns.config).UpdateOne(ns)
}

// Unwrap unwraps the NotificationSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ns *NotificationSchema) Unwrap() *NotificationSchema {
	_tx, ok := ns.config.driver.(*txDriver)
	if !ok {
		panic("ent: NotificationSchema is not a transactional entity")
	}
	ns.config.driver = _tx.drv
	return ns
}

// String implements the fmt.Stringer.
func (ns *NotificationSchema) String() string {
	var builder strings.Builder
	builder.WriteString("NotificationSchema(")
	builder.WriteString(fmt.Sprintf("id=%v", ns.ID))
	builder.WriteByte(')')
	return builder.String()
}

// NotificationSchemas is a parsable slice of NotificationSchema.
type NotificationSchemas []*NotificationSchema
