// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"persona/libs/database/ent/imageschema"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ImageSchema is the model entity for the ImageSchema schema.
type ImageSchema struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ImageSchema) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case imageschema.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ImageSchema fields.
func (is *ImageSchema) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case imageschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			is.ID = int(value.Int64)
		default:
			is.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ImageSchema.
// This includes values selected through modifiers, order, etc.
func (is *ImageSchema) Value(name string) (ent.Value, error) {
	return is.selectValues.Get(name)
}

// Update returns a builder for updating this ImageSchema.
// Note that you need to call ImageSchema.Unwrap() before calling this method if this ImageSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (is *ImageSchema) Update() *ImageSchemaUpdateOne {
	return NewImageSchemaClient(is.config).UpdateOne(is)
}

// Unwrap unwraps the ImageSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (is *ImageSchema) Unwrap() *ImageSchema {
	_tx, ok := is.config.driver.(*txDriver)
	if !ok {
		panic("ent: ImageSchema is not a transactional entity")
	}
	is.config.driver = _tx.drv
	return is
}

// String implements the fmt.Stringer.
func (is *ImageSchema) String() string {
	var builder strings.Builder
	builder.WriteString("ImageSchema(")
	builder.WriteString(fmt.Sprintf("id=%v", is.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ImageSchemas is a parsable slice of ImageSchema.
type ImageSchemas []*ImageSchema
