// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"persona/libs/database/ent/imageschema"
	"persona/libs/database/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ImageSchemaQuery is the builder for querying ImageSchema entities.
type ImageSchemaQuery struct {
	config
	ctx        *QueryContext
	order      []imageschema.OrderOption
	inters     []Interceptor
	predicates []predicate.ImageSchema
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ImageSchemaQuery builder.
func (isq *ImageSchemaQuery) Where(ps ...predicate.ImageSchema) *ImageSchemaQuery {
	isq.predicates = append(isq.predicates, ps...)
	return isq
}

// Limit the number of records to be returned by this query.
func (isq *ImageSchemaQuery) Limit(limit int) *ImageSchemaQuery {
	isq.ctx.Limit = &limit
	return isq
}

// Offset to start from.
func (isq *ImageSchemaQuery) Offset(offset int) *ImageSchemaQuery {
	isq.ctx.Offset = &offset
	return isq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (isq *ImageSchemaQuery) Unique(unique bool) *ImageSchemaQuery {
	isq.ctx.Unique = &unique
	return isq
}

// Order specifies how the records should be ordered.
func (isq *ImageSchemaQuery) Order(o ...imageschema.OrderOption) *ImageSchemaQuery {
	isq.order = append(isq.order, o...)
	return isq
}

// First returns the first ImageSchema entity from the query.
// Returns a *NotFoundError when no ImageSchema was found.
func (isq *ImageSchemaQuery) First(ctx context.Context) (*ImageSchema, error) {
	nodes, err := isq.Limit(1).All(setContextOp(ctx, isq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{imageschema.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (isq *ImageSchemaQuery) FirstX(ctx context.Context) *ImageSchema {
	node, err := isq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ImageSchema ID from the query.
// Returns a *NotFoundError when no ImageSchema ID was found.
func (isq *ImageSchemaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = isq.Limit(1).IDs(setContextOp(ctx, isq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{imageschema.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (isq *ImageSchemaQuery) FirstIDX(ctx context.Context) int {
	id, err := isq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ImageSchema entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ImageSchema entity is found.
// Returns a *NotFoundError when no ImageSchema entities are found.
func (isq *ImageSchemaQuery) Only(ctx context.Context) (*ImageSchema, error) {
	nodes, err := isq.Limit(2).All(setContextOp(ctx, isq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{imageschema.Label}
	default:
		return nil, &NotSingularError{imageschema.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (isq *ImageSchemaQuery) OnlyX(ctx context.Context) *ImageSchema {
	node, err := isq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ImageSchema ID in the query.
// Returns a *NotSingularError when more than one ImageSchema ID is found.
// Returns a *NotFoundError when no entities are found.
func (isq *ImageSchemaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = isq.Limit(2).IDs(setContextOp(ctx, isq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{imageschema.Label}
	default:
		err = &NotSingularError{imageschema.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (isq *ImageSchemaQuery) OnlyIDX(ctx context.Context) int {
	id, err := isq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ImageSchemas.
func (isq *ImageSchemaQuery) All(ctx context.Context) ([]*ImageSchema, error) {
	ctx = setContextOp(ctx, isq.ctx, "All")
	if err := isq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ImageSchema, *ImageSchemaQuery]()
	return withInterceptors[[]*ImageSchema](ctx, isq, qr, isq.inters)
}

// AllX is like All, but panics if an error occurs.
func (isq *ImageSchemaQuery) AllX(ctx context.Context) []*ImageSchema {
	nodes, err := isq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ImageSchema IDs.
func (isq *ImageSchemaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if isq.ctx.Unique == nil && isq.path != nil {
		isq.Unique(true)
	}
	ctx = setContextOp(ctx, isq.ctx, "IDs")
	if err = isq.Select(imageschema.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (isq *ImageSchemaQuery) IDsX(ctx context.Context) []int {
	ids, err := isq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (isq *ImageSchemaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, isq.ctx, "Count")
	if err := isq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, isq, querierCount[*ImageSchemaQuery](), isq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (isq *ImageSchemaQuery) CountX(ctx context.Context) int {
	count, err := isq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (isq *ImageSchemaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, isq.ctx, "Exist")
	switch _, err := isq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (isq *ImageSchemaQuery) ExistX(ctx context.Context) bool {
	exist, err := isq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ImageSchemaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (isq *ImageSchemaQuery) Clone() *ImageSchemaQuery {
	if isq == nil {
		return nil
	}
	return &ImageSchemaQuery{
		config:     isq.config,
		ctx:        isq.ctx.Clone(),
		order:      append([]imageschema.OrderOption{}, isq.order...),
		inters:     append([]Interceptor{}, isq.inters...),
		predicates: append([]predicate.ImageSchema{}, isq.predicates...),
		// clone intermediate query.
		sql:  isq.sql.Clone(),
		path: isq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (isq *ImageSchemaQuery) GroupBy(field string, fields ...string) *ImageSchemaGroupBy {
	isq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ImageSchemaGroupBy{build: isq}
	grbuild.flds = &isq.ctx.Fields
	grbuild.label = imageschema.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (isq *ImageSchemaQuery) Select(fields ...string) *ImageSchemaSelect {
	isq.ctx.Fields = append(isq.ctx.Fields, fields...)
	sbuild := &ImageSchemaSelect{ImageSchemaQuery: isq}
	sbuild.label = imageschema.Label
	sbuild.flds, sbuild.scan = &isq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ImageSchemaSelect configured with the given aggregations.
func (isq *ImageSchemaQuery) Aggregate(fns ...AggregateFunc) *ImageSchemaSelect {
	return isq.Select().Aggregate(fns...)
}

func (isq *ImageSchemaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range isq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, isq); err != nil {
				return err
			}
		}
	}
	for _, f := range isq.ctx.Fields {
		if !imageschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if isq.path != nil {
		prev, err := isq.path(ctx)
		if err != nil {
			return err
		}
		isq.sql = prev
	}
	return nil
}

func (isq *ImageSchemaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ImageSchema, error) {
	var (
		nodes = []*ImageSchema{}
		_spec = isq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ImageSchema).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ImageSchema{config: isq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, isq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (isq *ImageSchemaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := isq.querySpec()
	_spec.Node.Columns = isq.ctx.Fields
	if len(isq.ctx.Fields) > 0 {
		_spec.Unique = isq.ctx.Unique != nil && *isq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, isq.driver, _spec)
}

func (isq *ImageSchemaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(imageschema.Table, imageschema.Columns, sqlgraph.NewFieldSpec(imageschema.FieldID, field.TypeInt))
	_spec.From = isq.sql
	if unique := isq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if isq.path != nil {
		_spec.Unique = true
	}
	if fields := isq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, imageschema.FieldID)
		for i := range fields {
			if fields[i] != imageschema.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := isq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := isq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := isq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := isq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (isq *ImageSchemaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(isq.driver.Dialect())
	t1 := builder.Table(imageschema.Table)
	columns := isq.ctx.Fields
	if len(columns) == 0 {
		columns = imageschema.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if isq.sql != nil {
		selector = isq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if isq.ctx.Unique != nil && *isq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range isq.predicates {
		p(selector)
	}
	for _, p := range isq.order {
		p(selector)
	}
	if offset := isq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := isq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ImageSchemaGroupBy is the group-by builder for ImageSchema entities.
type ImageSchemaGroupBy struct {
	selector
	build *ImageSchemaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (isgb *ImageSchemaGroupBy) Aggregate(fns ...AggregateFunc) *ImageSchemaGroupBy {
	isgb.fns = append(isgb.fns, fns...)
	return isgb
}

// Scan applies the selector query and scans the result into the given value.
func (isgb *ImageSchemaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, isgb.build.ctx, "GroupBy")
	if err := isgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ImageSchemaQuery, *ImageSchemaGroupBy](ctx, isgb.build, isgb, isgb.build.inters, v)
}

func (isgb *ImageSchemaGroupBy) sqlScan(ctx context.Context, root *ImageSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(isgb.fns))
	for _, fn := range isgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*isgb.flds)+len(isgb.fns))
		for _, f := range *isgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*isgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := isgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ImageSchemaSelect is the builder for selecting fields of ImageSchema entities.
type ImageSchemaSelect struct {
	*ImageSchemaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (iss *ImageSchemaSelect) Aggregate(fns ...AggregateFunc) *ImageSchemaSelect {
	iss.fns = append(iss.fns, fns...)
	return iss
}

// Scan applies the selector query and scans the result into the given value.
func (iss *ImageSchemaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iss.ctx, "Select")
	if err := iss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ImageSchemaQuery, *ImageSchemaSelect](ctx, iss.ImageSchemaQuery, iss, iss.inters, v)
}

func (iss *ImageSchemaSelect) sqlScan(ctx context.Context, root *ImageSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(iss.fns))
	for _, fn := range iss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*iss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
