// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"persona/libs/database/ent/experienceschema"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExperienceSchemaCreate is the builder for creating a ExperienceSchema entity.
type ExperienceSchemaCreate struct {
	config
	mutation *ExperienceSchemaMutation
	hooks    []Hook
}

// Mutation returns the ExperienceSchemaMutation object of the builder.
func (esc *ExperienceSchemaCreate) Mutation() *ExperienceSchemaMutation {
	return esc.mutation
}

// Save creates the ExperienceSchema in the database.
func (esc *ExperienceSchemaCreate) Save(ctx context.Context) (*ExperienceSchema, error) {
	return withHooks(ctx, esc.sqlSave, esc.mutation, esc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (esc *ExperienceSchemaCreate) SaveX(ctx context.Context) *ExperienceSchema {
	v, err := esc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (esc *ExperienceSchemaCreate) Exec(ctx context.Context) error {
	_, err := esc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (esc *ExperienceSchemaCreate) ExecX(ctx context.Context) {
	if err := esc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (esc *ExperienceSchemaCreate) check() error {
	return nil
}

func (esc *ExperienceSchemaCreate) sqlSave(ctx context.Context) (*ExperienceSchema, error) {
	if err := esc.check(); err != nil {
		return nil, err
	}
	_node, _spec := esc.createSpec()
	if err := sqlgraph.CreateNode(ctx, esc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	esc.mutation.id = &_node.ID
	esc.mutation.done = true
	return _node, nil
}

func (esc *ExperienceSchemaCreate) createSpec() (*ExperienceSchema, *sqlgraph.CreateSpec) {
	var (
		_node = &ExperienceSchema{config: esc.config}
		_spec = sqlgraph.NewCreateSpec(experienceschema.Table, sqlgraph.NewFieldSpec(experienceschema.FieldID, field.TypeInt))
	)
	return _node, _spec
}

// ExperienceSchemaCreateBulk is the builder for creating many ExperienceSchema entities in bulk.
type ExperienceSchemaCreateBulk struct {
	config
	builders []*ExperienceSchemaCreate
}

// Save creates the ExperienceSchema entities in the database.
func (escb *ExperienceSchemaCreateBulk) Save(ctx context.Context) ([]*ExperienceSchema, error) {
	specs := make([]*sqlgraph.CreateSpec, len(escb.builders))
	nodes := make([]*ExperienceSchema, len(escb.builders))
	mutators := make([]Mutator, len(escb.builders))
	for i := range escb.builders {
		func(i int, root context.Context) {
			builder := escb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ExperienceSchemaMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, escb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, escb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, escb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (escb *ExperienceSchemaCreateBulk) SaveX(ctx context.Context) []*ExperienceSchema {
	v, err := escb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (escb *ExperienceSchemaCreateBulk) Exec(ctx context.Context) error {
	_, err := escb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (escb *ExperienceSchemaCreateBulk) ExecX(ctx context.Context) {
	if err := escb.Exec(ctx); err != nil {
		panic(err)
	}
}
