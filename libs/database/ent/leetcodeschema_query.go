// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"persona/libs/database/ent/leetcodeschema"
	"persona/libs/database/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LeetcodeSchemaQuery is the builder for querying LeetcodeSchema entities.
type LeetcodeSchemaQuery struct {
	config
	ctx        *QueryContext
	order      []leetcodeschema.OrderOption
	inters     []Interceptor
	predicates []predicate.LeetcodeSchema
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LeetcodeSchemaQuery builder.
func (lsq *LeetcodeSchemaQuery) Where(ps ...predicate.LeetcodeSchema) *LeetcodeSchemaQuery {
	lsq.predicates = append(lsq.predicates, ps...)
	return lsq
}

// Limit the number of records to be returned by this query.
func (lsq *LeetcodeSchemaQuery) Limit(limit int) *LeetcodeSchemaQuery {
	lsq.ctx.Limit = &limit
	return lsq
}

// Offset to start from.
func (lsq *LeetcodeSchemaQuery) Offset(offset int) *LeetcodeSchemaQuery {
	lsq.ctx.Offset = &offset
	return lsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (lsq *LeetcodeSchemaQuery) Unique(unique bool) *LeetcodeSchemaQuery {
	lsq.ctx.Unique = &unique
	return lsq
}

// Order specifies how the records should be ordered.
func (lsq *LeetcodeSchemaQuery) Order(o ...leetcodeschema.OrderOption) *LeetcodeSchemaQuery {
	lsq.order = append(lsq.order, o...)
	return lsq
}

// First returns the first LeetcodeSchema entity from the query.
// Returns a *NotFoundError when no LeetcodeSchema was found.
func (lsq *LeetcodeSchemaQuery) First(ctx context.Context) (*LeetcodeSchema, error) {
	nodes, err := lsq.Limit(1).All(setContextOp(ctx, lsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{leetcodeschema.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) FirstX(ctx context.Context) *LeetcodeSchema {
	node, err := lsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LeetcodeSchema ID from the query.
// Returns a *NotFoundError when no LeetcodeSchema ID was found.
func (lsq *LeetcodeSchemaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lsq.Limit(1).IDs(setContextOp(ctx, lsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{leetcodeschema.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) FirstIDX(ctx context.Context) int {
	id, err := lsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LeetcodeSchema entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LeetcodeSchema entity is found.
// Returns a *NotFoundError when no LeetcodeSchema entities are found.
func (lsq *LeetcodeSchemaQuery) Only(ctx context.Context) (*LeetcodeSchema, error) {
	nodes, err := lsq.Limit(2).All(setContextOp(ctx, lsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{leetcodeschema.Label}
	default:
		return nil, &NotSingularError{leetcodeschema.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) OnlyX(ctx context.Context) *LeetcodeSchema {
	node, err := lsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LeetcodeSchema ID in the query.
// Returns a *NotSingularError when more than one LeetcodeSchema ID is found.
// Returns a *NotFoundError when no entities are found.
func (lsq *LeetcodeSchemaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = lsq.Limit(2).IDs(setContextOp(ctx, lsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{leetcodeschema.Label}
	default:
		err = &NotSingularError{leetcodeschema.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) OnlyIDX(ctx context.Context) int {
	id, err := lsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LeetcodeSchemas.
func (lsq *LeetcodeSchemaQuery) All(ctx context.Context) ([]*LeetcodeSchema, error) {
	ctx = setContextOp(ctx, lsq.ctx, "All")
	if err := lsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LeetcodeSchema, *LeetcodeSchemaQuery]()
	return withInterceptors[[]*LeetcodeSchema](ctx, lsq, qr, lsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) AllX(ctx context.Context) []*LeetcodeSchema {
	nodes, err := lsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LeetcodeSchema IDs.
func (lsq *LeetcodeSchemaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if lsq.ctx.Unique == nil && lsq.path != nil {
		lsq.Unique(true)
	}
	ctx = setContextOp(ctx, lsq.ctx, "IDs")
	if err = lsq.Select(leetcodeschema.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) IDsX(ctx context.Context) []int {
	ids, err := lsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (lsq *LeetcodeSchemaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, lsq.ctx, "Count")
	if err := lsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, lsq, querierCount[*LeetcodeSchemaQuery](), lsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) CountX(ctx context.Context) int {
	count, err := lsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (lsq *LeetcodeSchemaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, lsq.ctx, "Exist")
	switch _, err := lsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (lsq *LeetcodeSchemaQuery) ExistX(ctx context.Context) bool {
	exist, err := lsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LeetcodeSchemaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (lsq *LeetcodeSchemaQuery) Clone() *LeetcodeSchemaQuery {
	if lsq == nil {
		return nil
	}
	return &LeetcodeSchemaQuery{
		config:     lsq.config,
		ctx:        lsq.ctx.Clone(),
		order:      append([]leetcodeschema.OrderOption{}, lsq.order...),
		inters:     append([]Interceptor{}, lsq.inters...),
		predicates: append([]predicate.LeetcodeSchema{}, lsq.predicates...),
		// clone intermediate query.
		sql:  lsq.sql.Clone(),
		path: lsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (lsq *LeetcodeSchemaQuery) GroupBy(field string, fields ...string) *LeetcodeSchemaGroupBy {
	lsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LeetcodeSchemaGroupBy{build: lsq}
	grbuild.flds = &lsq.ctx.Fields
	grbuild.label = leetcodeschema.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (lsq *LeetcodeSchemaQuery) Select(fields ...string) *LeetcodeSchemaSelect {
	lsq.ctx.Fields = append(lsq.ctx.Fields, fields...)
	sbuild := &LeetcodeSchemaSelect{LeetcodeSchemaQuery: lsq}
	sbuild.label = leetcodeschema.Label
	sbuild.flds, sbuild.scan = &lsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LeetcodeSchemaSelect configured with the given aggregations.
func (lsq *LeetcodeSchemaQuery) Aggregate(fns ...AggregateFunc) *LeetcodeSchemaSelect {
	return lsq.Select().Aggregate(fns...)
}

func (lsq *LeetcodeSchemaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range lsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, lsq); err != nil {
				return err
			}
		}
	}
	for _, f := range lsq.ctx.Fields {
		if !leetcodeschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if lsq.path != nil {
		prev, err := lsq.path(ctx)
		if err != nil {
			return err
		}
		lsq.sql = prev
	}
	return nil
}

func (lsq *LeetcodeSchemaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LeetcodeSchema, error) {
	var (
		nodes = []*LeetcodeSchema{}
		_spec = lsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LeetcodeSchema).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LeetcodeSchema{config: lsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, lsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (lsq *LeetcodeSchemaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := lsq.querySpec()
	_spec.Node.Columns = lsq.ctx.Fields
	if len(lsq.ctx.Fields) > 0 {
		_spec.Unique = lsq.ctx.Unique != nil && *lsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, lsq.driver, _spec)
}

func (lsq *LeetcodeSchemaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(leetcodeschema.Table, leetcodeschema.Columns, sqlgraph.NewFieldSpec(leetcodeschema.FieldID, field.TypeInt))
	_spec.From = lsq.sql
	if unique := lsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if lsq.path != nil {
		_spec.Unique = true
	}
	if fields := lsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, leetcodeschema.FieldID)
		for i := range fields {
			if fields[i] != leetcodeschema.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := lsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := lsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := lsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := lsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (lsq *LeetcodeSchemaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(lsq.driver.Dialect())
	t1 := builder.Table(leetcodeschema.Table)
	columns := lsq.ctx.Fields
	if len(columns) == 0 {
		columns = leetcodeschema.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if lsq.sql != nil {
		selector = lsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if lsq.ctx.Unique != nil && *lsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range lsq.predicates {
		p(selector)
	}
	for _, p := range lsq.order {
		p(selector)
	}
	if offset := lsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := lsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LeetcodeSchemaGroupBy is the group-by builder for LeetcodeSchema entities.
type LeetcodeSchemaGroupBy struct {
	selector
	build *LeetcodeSchemaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lsgb *LeetcodeSchemaGroupBy) Aggregate(fns ...AggregateFunc) *LeetcodeSchemaGroupBy {
	lsgb.fns = append(lsgb.fns, fns...)
	return lsgb
}

// Scan applies the selector query and scans the result into the given value.
func (lsgb *LeetcodeSchemaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lsgb.build.ctx, "GroupBy")
	if err := lsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeetcodeSchemaQuery, *LeetcodeSchemaGroupBy](ctx, lsgb.build, lsgb, lsgb.build.inters, v)
}

func (lsgb *LeetcodeSchemaGroupBy) sqlScan(ctx context.Context, root *LeetcodeSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lsgb.fns))
	for _, fn := range lsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lsgb.flds)+len(lsgb.fns))
		for _, f := range *lsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LeetcodeSchemaSelect is the builder for selecting fields of LeetcodeSchema entities.
type LeetcodeSchemaSelect struct {
	*LeetcodeSchemaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lss *LeetcodeSchemaSelect) Aggregate(fns ...AggregateFunc) *LeetcodeSchemaSelect {
	lss.fns = append(lss.fns, fns...)
	return lss
}

// Scan applies the selector query and scans the result into the given value.
func (lss *LeetcodeSchemaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lss.ctx, "Select")
	if err := lss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeetcodeSchemaQuery, *LeetcodeSchemaSelect](ctx, lss.LeetcodeSchemaQuery, lss, lss.inters, v)
}

func (lss *LeetcodeSchemaSelect) sqlScan(ctx context.Context, root *LeetcodeSchemaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lss.fns))
	for _, fn := range lss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
